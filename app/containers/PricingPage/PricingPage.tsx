import { View } from 'wiloke-react-core';

const PricingPage = () => {
  // const { data: dataDiscountAuto, getAutomaticDiscounts, error: errorDiscountAuto, loading: loadingDiscountAuto } = useGetAutomaticDiscounts();

  return (
    <View>
      {/* <Button
        onClick={() => {
          getAutomaticDiscounts();
        }}
      >
        Get auto discount
      </Button>
      {errorDiscountAuto && <View>{errorDiscountAuto}</View>}
      {loadingDiscountAuto && <View>Loading</View>}
      {dataDiscountAuto && <View tagName="pre">{JSON.stringify(dataDiscountAuto, null, 2)}</View>} */}
    </View>
  );
};

export { PricingPage };
